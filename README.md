# cilicili

iOS app project. GitHub Actions builds an unsigned IPA artifact on every push to `main` or `master`, and can also be started manually from the Actions tab.

The generated file is available from the `Build unsigned IPA` workflow run as the `bili-unsigned-ipa` artifact.
